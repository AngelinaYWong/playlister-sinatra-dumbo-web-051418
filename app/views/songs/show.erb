<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Artist</h1>
    <ul>
        <li><a href="/artists/<%= @song.artist.slug %>"><%= @song.artist.name %></a></li>
    </ul>
    <h1>Genres</h1>
    <ul>
      <% @song.genres.each do |genre| %>
        <li><a href="/genres/<%= genre.slug %>"><%= @song.genres.name %></li>
      <% end %>
    </ul>
  </body>
</html>
